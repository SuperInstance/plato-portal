[package]
name = "constraint-substrate"
version = "0.1.0"
edition = "2021"
description = "The 5 irreducible constraint primitives — Rust reference implementation"

[lib]
crate-type = ["lib"]

[dependencies]

[features]
default = ["std"]
std = []
