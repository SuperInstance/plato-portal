[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "constraint-substrate"
version = "0.1.0"
description = "The 5 irreducible constraint primitives — Python reference"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
